{% extends 'base.html.twig' %}

{% block title %}{{post.title}}{% endblock %}

{% block body %}
    <h1>{{post.title}}</h1>
    <p>Ecrit par {{ post.author }}</p>
    <div>
        {{ post.content | nl2br }}
    </div>
{% endblock %}