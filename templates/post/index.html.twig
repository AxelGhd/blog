{% extends 'base.html.twig' %}

{% block title %} Liste des articles {% endblock %}

{% block body %}
    <h1>Liste des articles</h1>
    <p>
        {# Ci-après les interpolations #}
        {{ twig_variable_name }}
        1 + 1 = {{ 1 + 1 }}

        {% if app.debug %}
            {# Toutes mes variables#}
            {{ dump() }}
        {% endif %}
    </p>
{% endblock %}

